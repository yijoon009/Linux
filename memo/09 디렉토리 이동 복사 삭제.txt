/ 시스템 루트 디렉터리
~ 홈 디렉터리

. 현재 디렉토리
.. 부모 디렉터리

절대경로 : /부터 이동
상대경로 : .을 이용해서 현재부터 이동

-------------------------------------------

<<복사 삭제>>>
cp -i : interactive 동일명 있으면 덮어쓸지 물어본다
cp -f : force 강제로 덮어쓴다
-r : recursive (디렉터리 포함)

cp -r 디렉터리까지 복사
rm -r 디렉터리까지 삭제

rm -rf 파일

---------------------------------------------

<이름 바꾸기>
rename a AAA a	: a를 AAA로 바꿔, 내용은 a야
mv b BBB	: b를 BBB로 옮겨(바꿔) (rename 기능 포함)(동일명 있어도 안물어보니까 조심해)